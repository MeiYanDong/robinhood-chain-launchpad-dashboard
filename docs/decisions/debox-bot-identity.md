# RH Launch Ledger｜DeBox Bot 身份决策

- 状态：`partial_confirmed`
- 最近更新：2026-08-31

## 已确认

- 正式显示名称：`RH Launch Ledger`
- 用户于 2026-08-31 明确选择该名称。

## 待确认

以下内容在 BotMother 创建前必须完成，但尚未获得用户确认：

- 头像素材；
- 简介；
- 支持入口。

推荐默认稿仅供预览，不视为已确认：

- 简介：`Robinhood Chain Launchpad 只读数据助手。查询成交量、用户费用、平台收入、排名与数据状态；不交易、不荐币。`
- 支持入口：公开仓库 GitHub Issues。
- 头像方向：简洁的 `RH` + Ledger/账本符号，不使用 Robinhood 官方商标或暗示官方背书。

## 创建边界

名称确认不等于授权创建 Bot。只有头像、简介、支持入口、专用账号、当前 `/mybots` 配额和不可删除风险全部确认后，才能单独通过 GATE-04 与 GATE-05。
