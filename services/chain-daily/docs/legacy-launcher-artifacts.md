# 旧发射器证据边界

Bankr 批量发射器的源码、测试、构建产物和带执行含义的 npm 命令已移除。

以下文件刻意保留，仅用于历史核对，不会被日度雷达读取：

- `runs/`：旧运行计划、回执与结果；
- `tokens*.csv`、`batch*.json`：旧输入样本；
- `assets/`：旧图片素材；
- 本机 `.env`：未读取、未修改，也不会被新程序加载。

如需进一步删除这些历史证据，应单独确认保留策略后再执行。
