# Changelog

## 0.20.5 - 2026-09-09

- PAIR 官方代币分页从每页 50 条调整为 API 已验证支持的 100 条，将约 2,411 个代币的请求数减半。
- 官方分页单请求上限固定为 10 秒并保留一次重试；后段页面故障不再把一轮 15 分钟刷新拖到 2 分钟以上才失败。

## 0.20.4 - 2026-09-09

- PAIR 核心榜刷新不再被可选的 GMGN 持币人数长时间拖住：单次持币请求上限由 20 秒收紧为 8 秒，失败时保留旧值或明确降级。
- 服务器定时刷新与日报改为直接调用只绑定本机的 collector，避免公共查询层拥堵导致内部任务 503；日报失败会在 2 分钟后自动重试。
- PAIR 刷新失败记录改用不含 URL 或凭证的分类原因，便于区分上游、分页、数据库忙和磁盘问题。

## 0.20.3 - 2026-09-09

- 将全链数据作为 PAIR 工作台的一级入口，与 PAIR 经营、PAIR Alpha、PAIR V2、资金闭环共用同一套导航、视觉和证据层级。
- 全链页新增四项直接判断、8 个核心指标、全部指标明细、四层趋势、风险提醒和股票代币覆盖；来源失败与日期落后继续显式降级。
- CashCat 继续退出一级导航和重点资产叙事；历史数据、只读 API 与旧链接兼容不删除。

## 0.20.2 - 2026-09-09

- 撤下以“全链 / CashCat”为中心的产品模板，公网根页恢复 PAIR 风格的发射台工作台。
- 一级导航聚焦市场总览、龙头与热度、PAIR Alpha、PAIR V2 和资金闭环；CashCat 不再作为重点资产或独立页面展示。
- 旧 `/market/`、`/alpha/` 与 `/assets/cashcat/` 链接安全跳转到对应工作台；CashCat 只读 API、报告、历史数据以及 PAIR 日交易量飞书告警继续保留。

## 0.20.1 - 2026-09-09

- 新增 PAIR 平台日交易量飞书告警：按最近两个完整 UTC 日比较，涨跌绝对值达到 10% 时发送。
- 告警只接受 PAIR 官方日度统计且要求连续两日完整；来源降级、缺日或零基线时失败关闭。
- 新增持久化去重 outbox、最多 5 次指数退避重试与只读健康接口；Webhook 继续仅保存在服务器环境中。

## 0.20.0 - 2026-09-09

- 将全链、发射台、Alpha 与 CashCat 日报重组为“今日 / 市场 / Alpha / 资产”四个用户任务；旧深度链接继续可用。
- 新增 `/api/product/today` 只读聚合契约，严格分开完整 UTC 日、实时快照与滚动口径，缺失值不补零。
- CashCat 成为首套重点资产模板，统一展示核心前提、实时行情、四维龙头位置、24H 价格与来源健康。
- 全链与 CashCat 可维护源码纳入同一仓库，CI 统一治理；生产进程、数据库、凭证和发布周期继续隔离。
- 公网根入口切至统一产品，保留全链 `/api/latest`、`/api/history`、`/api/snapshot` 与 CashCat 证据/API 路径。

## 0.19.3 - 2026-09-09

- 查询缓存刷新期间继续有界提供最后一次成功结果，并以响应头明确标记；超过 5 分钟仍无法更新时继续失败关闭，避免瞬时采集拥堵把页面清空。

## 0.19.2 - 2026-09-09

- 补齐总览、创建者发行与估值历史等完整运行时只读合同的预热，消除旧总览冷读仍可能超时的缺口。

## 0.19.1 - 2026-09-09

- 将龙头、PAIR Alpha、PAIR V2、项目方发行与资金闭环默认请求加入独立查询层预热，避免 collector 忙碌时首屏反复空榜。
- 查询层只读缓存容错窗口调整为 60 秒，未命中请求等待上限调整为 15 秒；响应继续保留观测时间、过期与来源状态。

## 0.19.0 - 2026-09-09

- 将龙头与热度、PAIR Alpha、PAIR V2、PAIR 资金闭环统一为“先结论、再对象、后证据”的阅读顺序。
- PAIR Alpha 与 V2 默认只展示前 5 个候选，每次再看 10 个；模型验证、历史回放、成交口径与链上凭证默认收起。
- PAIR V2 把已核验项目方发行放在候选榜之前；资金闭环首屏直接回答累计回购、累计销毁、待回购与待销毁。
- 合并重复来源提醒，补充判断可靠度说明，并把工程字段改为社区用户可理解的中文。
- 高优先级表格在手机端改为卡片，数据提醒不再覆盖关键判断；四个页面均消除横向溢出。

## 0.18.1 - 2026-09-08

- 用具体数据日期替换“闭合日”，所有交易量标题直接说明统计日期或日期范围。
- 将“相对平时”统一为“相较自身历史常态”，增加支持悬浮、键盘和手机点击的问号说明。
- 将 PAIR 对比结果改为溢价 / 折价，并明确参考价属于 PAIR、由 PONS 平台规模折算而来。
- 统一 Pons、Long、PAIR 在图例、卡片、表格和曲线中的颜色；首页七日图改用线性坐标并直接标注曲线名称与最新金额。
- 手机端比较卡使用动态字段名称，日期不再由 CSS 写死。

## 0.18.0 - 2026-09-08

### Changed

- 将发射台长页拆成“今日概览 / 平台经营 / PAIR vs PONS / 平台代币”四个可分享任务视图；
  默认页只保留三个关键结果、同日三平台对比和 7 日交易量走势。
- 平台活跃度改用“相对平时”的自然语言，主卡只显示当前日均与过去正常水平；样本、覆盖和日期
  收入折叠详情，经营结果另给出六列可读表。
- PAIR / PONS 主判断区只保留实际价、规模参考价、偏离和是否可参考；费用情景与七日实验默认
  折叠，相似样本少于 5 个或回测中位误差高于 50% 时不展示预测数字。
- 平台历史图与首页 7 日图为每个数据点提供日期和精确金额提示；四个任务视图共用原有服务端
  缓存并保持 60 秒自动读取。

## 0.17.0 - 2026-09-08

- 持久化 PONS 日线 OHLCV，以 UTC 日表格和逐点提示替代只可看趋势的估值曲线。
- 新增 PONS 七日影子模型：用链上广度、资金、市场与 Pons 平台成交匹配历史阶段，输出中枢、四分位区间、样本与滚动回测误差。
- PAIR 同时展示现价 PONS 锚和调整后 PONS 锚；持币地址增长只观察，当前模型权重固定为 0。

本项目按 Keep a Changelog 的结构记录用户可见行为和重要工程变化。版本号沿用 package.json。

## Unreleased

## 0.16.0 - 2026-09-08

### Added

- 发射台经营页新增 Pons、Long、PAIR 平台活跃度：7 日或 30 日的日均成交量与该平台自身此前
  同周期历史中位数比较，展示倍数、历史百分位和基准样本进度。
- 新增 7 日、30 日、上线以来三种平台交易量视图与 `/api/platform-activity` 审计接口；窗口不完整
  时仅展示已观测下限，三平台不在同一完整窗口时不计算份额。
- 日度历史从各平台首个已验证日期展示；缺失日和异常零值保留为断点，PAIR 与 Long 历史不足时
  明确显示“基准建立中”，不生成虚假倍数。

## 0.15.3 - 2026-09-08

### Changed

- 飞书通知改为 PAIR 项目方钱包硬白名单：当前只允许由官方代币 API 与 PAIR 发行交易共同核验的
  主发行钱包；Pons、Long、其它 PAIR 创建者及普通地址全部只入证据层，不再发送通知。
- 通知健康读模型公开 `pair_team_wallet_only` 策略名，发送前自动封存所有非白名单待发送项。

## 0.15.2 - 2026-09-08

### Fixed

- 飞书通知新增高置信项目创建者身份闸门：只有 PAIR / pons 官方发行事件直接给出的 indexed
  creator / deployer 可以触发提醒；Long 交易发送者、画像晋级和普通交易地址继续留在证据层，
  不再占用通知注意力。
- 每次投递前自动封存不符合身份规则的历史未发送项，同时保留项目、活动和历史投递记录以便审计。

## 0.15.1 - 2026-09-08

### Changed

- PAIR / PONS 相对估值面板直接展示基础公式、六项输入、来源、观测时间和逐项代入结果；共同闭合日窗口落后时明确标记天数。
- 基础相对估值中枢与 90% / 80% 费用分配情景分开展示；日估值区间明确为中间 50%，实际溢价与折价使用不同状态色。

## 0.15.0 - 2026-09-07

### Reliability

- 查询进程与采集进程同机隔离；有界缓存、并发和响应大小，采集超时不阻塞静态页面，过期缓存明确返回不可用。
- 异常零值保留原始证据并隔离出份额/估值；三强 API 分别报告快照新鲜度、平台完整性与估值可用性，逐指标标记过期。
- RPC 批量失败后有限降级到单条读取，校验链 ID 并固定区块；备用地址错误不暴露凭证。
- 高频监控迁入独立 SQLite；历史市场点只存回放所需数值，完整当前快照、信号、事件及旧数据库保留。

### Added

- PAIR 普通代币榜、PAIR V2、跨代 Alpha 与项目方发币列表现在直接展示官方代币接口
  `pairs[].quoteToken` 对应的底层配对资产；同资产多池按合约地址去重，多个资产分别列出。
- 普通 PAIR 快照把配对资产写入 SQLite，旧数据库自动增加兼容字段；接口未返回可核验数据时
  前端明确显示“未索引”，不会默认猜成 SPY、WETH 或其他资产。

## 0.14.7 - 2026-09-07

### Fixed

- PAIR V2 看板改为内存热读与单槽位崩溃恢复检查点，链上/热市场轮询不再每 8–15 秒同步读取、
  解析并追加整份多 MB 看板 JSON，消除监控进程的主要 SQLite I/O 阻塞源。
- 旧的 2,048 份看板缓存原样保留且不再参与实时写路径；全量市场轮询才更新一次单槽位检查点，
  因而止住数据库继续无界膨胀，同时保持迁移可回滚。

## 0.14.6 - 2026-09-07

### Changed

- 实时 Alpha 优先于历史回补：DEV 买入游标落后超过 10,000 块时，服务记录明确的数据缺口并
  快速前移到当前确认区块；旧区间不生成飞书通知，避免数小时追赶期间漏掉当前交易。
- 数据缺口保存在服务端状态账本，可由独立离线任务日后回补；当前实时链路不再被历史债务阻塞。

## 0.14.5 - 2026-09-07

### Fixed

- 当 2,000 块买入扫描仍遭遇 RPC 限流时，自动按 1,000、500、250、125 块逐级缩小并退避；
  任一子区间成功后立即保存游标，热点区块不再让后续扫描永久停住。
- 代币元数据查询遇到 429、5xx、网络或超时错误时不再把资产永久缓存为“不支持”，而是把本轮
  标记为失败并留待安全重试，避免限流造成静默漏单。

## 0.14.4 - 2026-09-07

### Fixed

- DEV RPC 请求在客户端串行化并至少间隔 750ms，避免日志、receipt、交易与区块批量读取在同一
  瞬间打满官方公共 RPC，导致 `HTTP 429` 后买入游标停滞。
- 生产 DEV 轮询与 PAIR V2 的 8 秒轮询错峰为 11 秒；既保留近实时发现，也降低两个自治扫描器
  同时突发请求的概率。

## 0.14.3 - 2026-09-07

### Fixed

- DEV 买入游标落后时改为每轮有界分段追赶，成功一段就持久化游标，不再让一次超大
  `eth_getLogs` 失败后永久卡在同一区间。
- 历史追赶期间只写入交易证据账本，不产生飞书买入通知；追到当前确认区块后才恢复实时提醒。
- 通知时间改用链上交易或发行事件自身时间，延迟采集的旧事实会按有效期封存，不会伪装成新信号。

## 0.14.2 - 2026-09-07

### Changed

- 根据 `0.14.1` 上线后约 10 小时的真实投递回读继续收紧注意力预算：DEV 晋级只保留在
  面板，不再发送飞书；同一 DEV 同类事件冷却从 1 小时增至 6 小时。
- 普通新发币使用 20 分钟投递间隔进行聚合，高置信买入可立即突破该间隔；全局预算从每小时
  6 条信号降至 3 条，信号有效期调整为 45 分钟。

## 0.14.1 - 2026-09-07

### Fixed

- DEV 飞书未配置时不再持续制造待发送记录；历史事件继续保存在项目与交易事实表，不把数据留档
  冒充通知债务。
- 旧通知超过 15 分钟后转为不可投递封存，恢复 Webhook 时不会重放历史队列。

### Changed

- DEV 通知只保留通过反垃圾门槛的 `proven` 创建者，买入还必须指向已核验发射台代币。
- 同一 DEV 同类动作采用 60 分钟冷却；每小时最多 6 条信号，每次最多 3 条合并成一条飞书消息。
- 通知健康状态分开报告当前可投递队列和已封存历史噪声。

## 0.14.0 - 2026-09-06

### Added

- 新增独立 `/pair-alpha/` 跨代机会雷达，统一发现 PAIR 官方目录中的 V1、V2 与后续可识别
  代际，同时保留当前 release 事件作为更强的发行身份凭证。
- 新增点火观察、回踩观察、研究候选、过热勿追、风险停止等互斥动作通道；所有输出保持
  Shadow，明确不连接钱包、不产生仓位或自动交易。
- 新增首次发现、首次点火和动作迁移账本，以及 5m、30m、2H、6H、24H 固定入口回放。
- 新增 `/api/pair/alpha`、健康、单币详情与受限手动刷新接口，并补齐 Nginx 产品前缀路由。

### Changed

- PAIR 官方全量目录每 60 秒同步；V2 和活跃、近期或达到经济门槛的 V1 项目进入行情候选，
  其中最多 72 枚热候选每 15 秒更新 canonical 池短周期行情。
- 多池代币分列官方 24H、主池 24H、canonical 池成交毛额、流动性加权共识价格与跨池价差；
  没有地址级资金流证据时，净 Quote 流入保持 `UNKNOWN`。
- 过热闸门加入 6H/24H 爆发阈值，避免已经大幅上涨的项目继续被标成早期埋伏候选。
- 新鲜度从采集完成时计算，避免冷启动耗时超过阈值后立刻重复全量刷新；低于经济流动性门槛
  的尘埃池不再制造天文级跨池价差。

## 0.13.2 - 2026-09-06

### Fixed

- PAIR V2 主入口从“核心 DEV 表现筛选”纠正为“PAIR 项目方发币”，只统计已核验的 PAIR
  主发行钱包，不再把市场强样本创建者误写成 PAIR 项目方。
- `$PAIR` 明确标为官方协议代币；同一项目方钱包发行但没有独立官方确认的代币单独标注，
  不把钱包关联冒充品牌背书。

### Added

- 新增只读接口 `/api/dev-monitor/pair-team-launches`，返回项目方钱包证据、官方确认数量、
  同钱包发行数量以及逐币市场与链上发行数据。

## 0.13.1 - 2026-09-06

### Fixed

- PAIR DEV 筛选按钮明确显示的是“发币数量”，DEV 履历明确为全链统计，避免把代币行数误读为
  唯一创建者数量或 PAIR 单平台历史。

## 0.13.0 - 2026-09-06

### Added

- PAIR V2 页面新增“PAIR DEV 新币流”，逐枚展示链上发行事件中的项目创建者、DEV 履历、
  价格、市值、流动性、24H 成交、持币地址、确认区块与发行交易。
- 新增只读分页接口 `/api/dev-monitor/pair-launches`，仅公开 PAIR V2 已有链上公开归属，
  支持全部、重点、重复命中与强样本四种筛选。

### Changed

- DEV 雷达不再只显示聚合计数；PAIR V2 项目创建者关系进入前端可查，但跨平台完整地址库、
  买入行为账本、飞书 outbox 与任何服务端配置仍不公开。

## 0.12.1 - 2026-09-06

### Fixed

- DEV 雷达把 Webhook 字符串存在显示为“飞书已配置”，不再把它误写成已经收到业务成功回执。

## 0.12.0 - 2026-09-06

### Added

- 服务端常驻 DEV 雷达：用 PAIR V2、pons v1/v2 与 Long 各自的发行事件发现创建者。
- 候选、重复有效、已验证活跃三层地址画像；重复发币本身不构成重点监听依据。
- 基于成功 receipt、目标代币净流入与支付腿净流出的高置信买入识别。
- 独立来源游标、首次无历史轰炸基线、去重 outbox 与指数退避的飞书通知链路。
- PAIR V2 官方 poolId 的 DexScreener 5m/1H/6H 成交、买卖笔数、价格变化与流动性补充源。
- 质量门槛、关注度、早期时机、热度、平台风险、单币风险和证据完整度相互独立的 Shadow Alpha 模型。
- 每个模型版本和项目的首次信号账本，以及 5m、30m、2H、24H 扣平台费结果回放。

### Changed

- PAIR V2 页面新增 DEV 雷达聚合健康状态；完整地址与行为流水仍留在服务端。
- `/pair-v2/` 重构为候选漏斗、短周期证据矩阵与模型验证面板；协议费用和回购动作降为次级信息。
- 飞书告警只在研究资格成立、进入过热或单币风险跃升时触发，不把单纯成交放大当成 Alpha。

## 0.11.0 - 2026-09-05

### Added

- 独立 `/pair-v2/` 实时监控页，分开展示当前 canonical release、三种发行模式、市场快照、链上费用归集、回购销毁、持有人领取和升级事件。
- 当前 release 的标准化 SQLite 账本、短重组回扫、回购桶逐资产读取，以及 8 秒链上/60 秒市场双频后台监控。
- 发现、确认、热度、风险与数据置信度五套独立判断；按模式和币龄建立比较组，不输出黑箱综合分。
- 状态跃迁式飞书告警队列；默认关闭，只有服务端配置官方 Feishu/Lark webhook 后才发送。
- PAIR V2 健康、事件、单币详情、缓存读取和受限手动刷新 API。

### Changed

- 估算手续费、理论模式份额、链上实际执行与未知值在接口和页面中分层展示，禁止用成交量反推实际回购。
- 当前生产合约图未完成源码与公开审计闭环时，风险保持高位，但不会覆盖早期机会信号。

## 0.10.0 - 2026-09-05

### Added

- 独立 `/pair-flow/` 资金闭环页面，逐笔展示团队执行地址的 PAIR 回购和死亡地址销毁交易。
- 交易详情缓存、事件账本和 FIFO 回购—销毁关联，区分已回购、已销毁、已回购待销毁与政策口径待回购资金。
- `GET /api/pair/flow/events` 分页筛选接口，以及每 5 分钟后台刷新资金闭环的 systemd 定时器。

### Changed

- 发射台页只保留资金闭环摘要入口；完整明细、证据链接和资金状态集中到独立页面。
- PAIR 资金闭环在无人打开网页时也会持续刷新；页面保持 60 秒只读轮询。

## 0.9.0 - 2026-09-04

### Added

- 统一的 `/leaders/` 龙头、链热度、代币交易压力与同角色相对估值入口，并保留 `/launchpads/`、`/cashcat/` 和全链日度雷达的独立职责。
- 结构龙头与断崖龙头双层判定；市值、主池流动性、四档成交窗口与持币地址不合成黑箱总分。
- 用户广度、交易强度、成本压力、跨链注意力四维链热度，以及缺价格动量时最高只判“高热”的代币压力模型。
- PAIR、Long 发射代币的同角色相对估值区间；缺失或过期输入保持 `UNKNOWN`，不补零、不输出目标价。
- `/api/intelligence`、健康与受限刷新接口，以及主链雷达和 CashCat 的只读聚合源。

### Changed

- 全链、龙头与热度、发射台、CashCat 日报四个页面使用同一组入口导航。
- CashCat 仅叙事源降级时，不再错误降低只消费其链上行情的情报模块状态。

## 0.8.0 - 2026-09-03

### Added

- PAIR 相对估值：以 PONS 当前价格、双方销毁调整后的有效供应量和最近 7 个共同闭合日平台成交量比计算同倍数锚点。
- 相对估值第 25–75 百分位区间、公开手续费分配比例独立情景、实际价格偏离和置信度。
- 紧凑估值历史表、只读当前值/七日历史 API，以及首页 60 秒缓存轮询和实际价/估值走势图。

### Changed

- 相对估值至少需要 5 个共同闭合日和 30 分钟内的 PONS 价格；缺失、过期或无效输入直接标记不可用，不用 0 猜填。
- PAIR 实际价格只用于偏离对照，公开回购比例只进入独立情景，不参与估值中心。

## 0.7.1 - 2026-09-03

### Added

- 代表币价值表新增当前美元价格，PONS、Long 动态龙头与 PAIR 均展示数据源直接报告值。

### Changed

- PAIR/Long 代币快照持久化价格，并为既有 SQLite 表执行幂等增量迁移。

## 0.7.0 - 2026-09-03

### Added

- Pons、Long、PAIR 三强对比首屏，分开展示代表币价值、平台经营和回购核验。
- Pons 与 PAIR 官方闭合日成交量采集器，以及仅在三家同日数据齐全时计算的市场份额。
- PONS/PAIR 来源市值与销毁调整市值双口径、Robinhood Chain 链上累计销毁余额。
- 协议收入应计/实收、理论回购预算/实际回购、回购后留存/净利润的独立状态模型。
- `/api/economics` 健康、快照、来源与刷新接口，以及发布后运行合同验证。

### Changed

- 首屏默认进入“三强对比”，PAIR 名称统一，不再出现误写的 `PRIA`。
- Pons 官方 Dune 日成交量成为同日成交量比较的优先来源；费收范围仍单独标注。
- Long 代表币改为当前市值龙头动态选择；回购字段明确为“不适用”而不是 `$0`。

## 0.6.0 - 2026-09-01

### Added

- Long 代币雷达：市值、流动性、滚动 24H 交易量、持币地址四项独立 Top 5。
- GMGN `longxyz` 活跃样本采集与 LongLauncher `LaunchCreated` 链上归属核验缓存。
- Long 实时榜、08:00 日报、健康、来源和刷新 API，以及独立 systemd 定时任务。

### Changed

- 首页数据集切换扩展为“平台总览 / PAIR 代币 / Long 代币”，代币页继续复用同一套紧凑榜单交互。
- PAIR 与 Long 的 SQLite 存储共用可命名空间化的数据雷达基座，数据表仍相互隔离。

## 0.5.0 - 2026-09-01

### Added

- PAIR 代币雷达：市值、全部官方池流动性深度、滚动 24H 交易量、持币地址四项独立 Top 5。
- PAIR 官方 API 全分页采集、GMGN 持币地址缓存、经济活跃门槛与独立 SQLite 历史快照。
- PAIR 实时榜 15 分钟刷新和北京时间 08:10 日报固化的 systemd units。
- PAIR 健康、排行、来源、日报与手动刷新 API，并纳入发布后运行验证。
- 公开 GitHub 仓库、Biome 格式/lint、全源码覆盖率门槛与 GitHub Actions CI 定义。
- HTTP、DashboardService、SQLite 和只读运行验证的关键路径测试。
- 可测试的只读运行验证器与发布后 runbook。
- DeBox Bot 的详细 plan、todo、技术决策和 Phase 1 故事卡。
- RHC Launch Ledger DeBox Bot 的本地无凭证原型：六个只读命令、受约束自然语言 QueryPlan、Ledger v1 合同、fake Long Polling、幂等发送、匿名聚合遥测和 localhost health。
- `GET /api/meta` 只读能力清单与对应运行时合同校验。
- Bot 专用测试、fixture replay、fake 启动和性能/压力基准命令。

### Changed

- 首页增加“平台总览 / PAIR 代币”数据集切换；数据说明继续按需展开，不增加口号或说明性首屏。
- HTTP 路由从进程入口拆为可测试 handler；server.ts 只负责组合与生命周期。
- DashboardService 支持 collector、clock 和安全日志依赖注入。
- 测试发现规则同时覆盖根目录与 `test/bot`，防止新增子目录后静默漏跑既有测试。

### Security

- 公共错误使用稳定错误码，不再返回原始异常消息。
- 公开 source/run/warning 信息经过脱敏；失败详情只保留内部错误类别。
- Bot 只允许固定 GET 路径，拒绝重定向、任意 URL/方法、`/api/refresh`、主数据库和采集器依赖；日志与遥测不保存原始消息或稳定身份。

## 0.4.0

- 增加 Long 官方只读数据候选与 LetsCash 官方日序列优先级。
- 当前 README 记录该版本尚未部署到既有生产实例。
